import 'package:flutter/material.dart';

class PostInfoWidget extends StatelessWidget {
  const PostInfoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
